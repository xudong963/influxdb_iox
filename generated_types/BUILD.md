This crate contains protobuffers definitions and generated code.

To rebuild the protobuf generated code, go into the `generate` directory and run `cargo run`

```shell
( cd generate && cargo run )
```
